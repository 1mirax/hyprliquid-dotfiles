-- Programs and the modifier key, shared by every other module.
--
-- Deliberately global rather than local: each module is a separate chunk, and
-- a local here would not be visible from keybinds.lua.
mod = "SUPER"
term = "kitty"
menu = "fuzzel"
files = "nemo"
browser = "firefox"
